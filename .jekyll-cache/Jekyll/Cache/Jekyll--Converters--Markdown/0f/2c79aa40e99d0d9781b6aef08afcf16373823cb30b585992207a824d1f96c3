I"�<h4 id="podfild中添加如下代码重新pod-install"><code class="language-plaintext highlighter-rouge">Podfild</code>中添加如下代码重新<code class="language-plaintext highlighter-rouge">pod install</code></h4>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>post_install do |installer|
  installer.pods_project.targets.each do |target|
    if target.respond_to?(:product_type) and target.product_type == "com.apple.product-type.bundle"
      target.build_configurations.each do |config|
          config.build_settings['CODE_SIGNING_ALLOWED'] = 'NO'
      end
    end
  end
end
</code></pre></div></div>
:ET